# Default Homebrew MySQL server config
[mysql]
# Only allow connections from localhost
bind-address = 127.0.0.1
user=root
password=master

[mysqldump]
# Only allow connections from localhost
bind-address = 127.0.0.1
user=root
password=master